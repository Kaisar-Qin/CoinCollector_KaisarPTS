using UnityEngine;

public class EnemyChildhren : Enemy
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public override void Serang()
    {
        Debug.Log("Children Menyerang");
    }
}
